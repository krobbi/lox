// Lynx
// Lox preprocessor.

// Run Lynx from arguments and return an exit status code.
fun main(args) {
	var log = Log();
	log.logError("Hello, Lynx!");
	
	if (log.hasErrors()) {
		log.flush();
		//return 1; // TODO: Uncomment when errors are no longer expected.
	}
	
	return 0;
}

callMain(main);
