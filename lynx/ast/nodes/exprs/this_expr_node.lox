// #import "/ast/node.lox"

// A this expression node of an AST.
class ThisExprNode < Node {
	// Initialize the this expression node from its span.
	init(span) {
		super.init(span);
	}
	
	// Visit the this expression node with a visitor.
	visit(visitor) {
		return visitor.visitThisExpr(this);
	}
}
