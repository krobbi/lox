// A record of a log.
class LogRecord {
	// Initialize the log record from its message.
	init(message) {
		// The log record's message.
		this._message = message;
	}
	
	// Represent the log record as a string.
	toString() {
		return "[Error] " + this._message;
	}
}
