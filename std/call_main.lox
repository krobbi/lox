// Call a main function with arguments and exit if a non-zero exit status code
// was returned.
fun callMain(main) {
	// TODO: Implement list collection and call main with an argument list.
	var status = main(__argc(), __argv);
	
	if (status != 0) {
		__exit(status);
	}
}
